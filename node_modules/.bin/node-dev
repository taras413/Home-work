#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dev/node-dev.sh" "$@"
  ret=$?
else 
  node  "$basedir/../dev/node-dev.sh" "$@"
  ret=$?
fi
exit $ret
